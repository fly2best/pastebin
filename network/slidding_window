
滑动窗口的两个作用:

1.  提供高效的可靠传输.

    一个简单的正幂昂确认协议会浪费大量的网络带宽, 因为它在受到确认之前必须推迟下一个分组的发送.
    滑动窗口协议, 能更好的利用网络带宽, 因为其允许发送方, 在等待一个确认到达之前发送多个分组.

2.  可以进行端到端的流量控制.

    如果接受方的缓冲区快满了, 为避免受到的数据存不下, 就会发送较小的窗口通告.


