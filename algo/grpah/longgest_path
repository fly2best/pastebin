
最长路径

在一般图上的最长路径, 似乎只能枚举.
最长路径不像最短路径一样, 有很好的性质, 不能转化为子问题.
但是在两种图上, 去可以有效求解.

1. 无向无环图
    这个可以用两遍dfs或者树形dp来做.

2. 有向无环图
    可以用动态规划做.
    dp[v] 表示从u可达的最长路径权值
    dp[u] = max(l(u,v) + dp[v])
    最长路径 = max(dp[u]) for all u in |V|
    可以按照拓扑排序的逆序来算, 也可以直接dfs, 记忆化搜索.

先mark一下, 改天把这篇文章总结为博文.
