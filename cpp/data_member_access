
对与静态成员的访问效率和全局变量 通过指针或者通过对象访问 的效率是一样的.

对与非静态成员.
如果不涉及到虚继承, 那么其效率, 和全局变量访问是一样的, 其成员的偏移在编译时候就已经决定了.  无论是单一继承还是多重继承.
如果涉及到虚继承, 那么访问基类中的成员, 会用虚函数表, 获取成员在变量内的偏移, 因此会有性能损失.

可以参照 ./data_member_access_with_virtual_inherit.cc
